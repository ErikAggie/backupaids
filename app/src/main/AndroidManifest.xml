<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="peterson.ttu.edu.backupaids">

    <uses-permission
        android:name="android.permission.RECORD_AUDIO"
        android:required="true" />
    <!-- Bluetooth permissions-->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:required="true" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:required="true"/>
    <uses-permission
        android:name="android.permission.MODIFY_AUDIO_SETTINGS"
        android:required="true" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:required="true" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        android:required="true"/>
    <!-- These are for WiFi direct
    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE"
        android:required="true" />
    <uses-permission
        android:name="android.permission.CHANGE_WIFI_STATE"
        android:required="true" />
    <uses-permission
        android:name="android.permission.INTERNET"
        android:required="true" />-->

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.TabbedMain"
            android:label="@string/title_activity_tabbed_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.headsetSetup.PresetSetupActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".service.LocalSoundService"
            android:exported="false" />
        <service
            android:name=".service.StreamSoundService"
            android:exported="false" />
        <service
            android:name=".service.RemoteSoundService"
            android:exported="false" />

        <receiver android:name=".service.LocalPlaybackBroadcastReceiver"/>
        <receiver android:name=".service.RemotePlaybackBroadcastReceiver"/>
    </application>

</manifest>